auth:
  google:
    client-id:
    client-secret:
    scope:
      - email
      - profile
    token-req-url : https://oauth2.googleapis.com
  kakao:
    client-id:
    client-secret:
    scope:
      - profile_nickname
      - profile_image
      - account_email
  redirect-url: http://localhost:3000

custom:
  jwt:
    secret: jwt시크릿
